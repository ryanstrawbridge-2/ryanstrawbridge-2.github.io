# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables — secrets must never be committed
.env
.env.local
.env.*.local
.env.production

# tina (auto-generated GraphQL client and types)
tina/__generated__/

# local photo staging — full-resolution originals, resized into src/assets before use
portfolio-photos/

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# LaTeX build artifacts (the .tex and the .pdf are the deliverables)
resume/*.aux
resume/*.log
resume/*.out
resume/*.fls
resume/*.fdb_latexmk
resume/*.synctex.gz
